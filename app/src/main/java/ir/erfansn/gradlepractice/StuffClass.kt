package ir.erfansn.gradlepractice

import kotlinx.datetime.Instant

class StuffClass {

    fun doStuff() {
        // Because of the use of `api` configuration, we can access Kotlin date dependency
        Instant.DISTANT_FUTURE

        // We can access it only compile time
        // MyClass()
    }
}